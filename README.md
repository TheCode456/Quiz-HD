# Quiz-HD

Quiz-HD is a **Python-based local quiz application** built using **CustomTkinter**.  
It focuses on providing a clean UI experience that matches the **modern Windows rounded-corner design**.

## Features

- **CustomTkinter UI** for a modern interface
- **Multi-user system**
- **Randomized quiz questions**
- **XP and level system**
- **User profile management**
- **Statistics dashboard**
- **Question sets management system [(v1.5)](https://github.com/TheCode456/Quiz-HD/releases/tag/v1.5)**

## Installation

- **Cloning**: Clone the files in your system using the following git command:
```bash
git clone https://github.com/TheCode456/Quiz-HD.git
```
Go to the folder:
``` bash
cd Quiz-HD
```
- **Installing Modules**: Use the requirements.txt to install the required modules in your Python environment:
``` bash
pip install -r requirements.txt
```
## Running Program
Complete the installation steps and run the following command in the terminal. The directory must be the Quiz-HD folder:

``` bash
python main.py
```
## Project Structure

The project is divided into multiple files, each responsible for a specific task.

### Startup Files
- **files_check.py**  
  Runs at startup to ensure that all required files exist.

- **autocorrect.py**  
  Validates and corrects the format of JSON files that store user data.

### User System
The login system uses `user.json` files to:
- Display available users
- Authenticate using passwords
- Add new users

### Main Interface
The main application contains a **sidebar with multiple tabs**, including:

- **Dashboard**  
  Displays user statistics such as:
  - Current Level
  - Last quiz result

- **Quiz Button**  
  Allows the user to start a quiz.

### Quiz System
The `Quiz.py` file manages quiz functionality.

Key features:
- Questions are **randomized using `random.shuffle()`**
- A single **content_frame** is reused to display different screens
- A helper function `clear_frame(frame)` removes existing widgets before loading new content

### User Profile
Users can:
- Change their **profile picture**
- **Update their password**
- **Delete their account**

### XP & Level System
The level is calculated using the formula:

```python
int(math.sqrt(xp / 100)) + 1
```

## Assets and Credits
- default.png - icon from **Flaticon** <br>
source link: https://www.shareicon.net/download/128x128//2016/05/24/770085_man_512x512.png

- Question sets: generated by AI
- JSON file validation (in upload question): code by ChatGPT
